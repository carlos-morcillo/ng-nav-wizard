import { NextStepButtonDirective } from './next-step-button.directive';

describe('NextStepButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new NextStepButtonDirective();
    expect(directive).toBeTruthy();
  });
});
