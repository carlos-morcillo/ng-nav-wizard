import { StepTriggerDirective } from './step-trigger.directive';

describe('StepTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new StepTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
