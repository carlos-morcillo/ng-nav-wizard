import { StepperNavDirective } from './stepper-nav.directive';

describe('StepperNavDirective', () => {
	it('should create an instance', () => {
		const directive = new StepperNavDirective();
		expect(directive).toBeTruthy();
	});
});
