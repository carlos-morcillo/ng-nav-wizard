import { NgStepperNavDirective } from './ng-stepper-nav.directive';

describe('NgStepperNavDirective', () => {
  it('should create an instance', () => {
    const directive = new NgStepperNavDirective();
    expect(directive).toBeTruthy();
  });
});
