import { PrevStepButtonDirective } from './prev-step-button.directive';

describe('PrevStepButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PrevStepButtonDirective();
    expect(directive).toBeTruthy();
  });
});
